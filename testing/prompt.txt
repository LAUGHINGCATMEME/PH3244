This is my folder structure:

MAINDIR/ 
MAINDIR/1/
MAINDIR/2/
MAINDIR/3/
MAINDIR/scripts/
MAINDIR/scripts/labtools.py

Most of the things I will ask you to do will be related to making and modifying labtools.py only.
Now for any experiment, for example for experiment 1, the structure is the followng: 
1/
1/data
1/data/[OBSERVATION NAME].csv
1/data/[ANOTHER OBERVATION NAME].csv
1/parsed_data
1/1.tex
1/calculations.py
Here 1.tex is the main latex file and calculations.py is the main python file for that experimetnt.

For any .csv file, this is the stucture of it, if, it is as data/[OBSERVATION NAME].csv:
[VARIABLE1] (UNIT1),[VARIABLE2] (UNIT12, [VARIABLE3] (UNIT3), [VARIABLE4] (UNIT4) 
[LEAST COUNT OF VARIABLE 1],[LEAST COUNT OF VARIABLE 2],[LEAST COUNT OF VARIABLE 3],[LEAST COUNT OF VARIABLE 4] 
DATAPPOINT_1_1,DATAPOINT_1_2,DATAPOINT_1_3,DATAPOINT_1_4
DATAPPOINT_1_1,DATAPOINT_1_2,DATAPOINT_1_3,DATAPOINT_1_4
DATAPPOINT_1_1,DATAPOINT_1_2,DATAPOINT_1_3,DATAPOINT_1_4
DATAPPOINT_1_1,DATAPOINT_1_2,DATAPOINT_1_3,DATAPOINT_1_4

for example here is a example in my data:
❯ cat data/For I = 1 Amp.csv 
Voltage (V),rhs (cm),lhs (cm)
10,0.1,0.1
200,4.5,14.7
180,4.9,14.4
150,5.9,14.5
120,6.6,14.6
100,7.2,14.3

after I import labtools.py to a experiment I want it to have the following functions which I can run in calculations.py:

STEP - 1
Unit dictionary
Make a unit dictionaly with has all the valid units and the conversion factor to convert them into SI.
for example: 
{"cm":0.01
"m":1
"mm":0.001
"A":1
"mA"0.001
"g":0.001
"mg":0.000001
"Kg:1
"sec":1
} I have just written example but make a proper dictionary which contains almost all measurement like ohms kilo ohms, micro Amp, coloumb etc etc



def import_data()
	this function checks for csv files in data/, and imports the, checks weather the csv format is correct or not, if somthign happens then it hals here . the csv should be now imported as pandas dataframe with the csv file name (OBSERVATION NAME) as the variable containg the data of that paticular csv
So after this function is ran I have multiple panda data frame with name 
[OBSERVATION NAME]
[ANOTHER OBERVATION NAME]

STEP - 2
def unit_check()
	this function now checks the units of the data imported. this functions check weather the [UNITX] associated with [VARIABLEX] is in the dictionary or not. If a [VARIABLE] doesnt have a valid unit associated with it, then the program halts here/ 

STEP - 3
def unit_converter()
	now since all the variable units are verified this function converts all the data sets into SI units using the dictionary made in STEP - 1
After this function the Panda data set ([OBSERVATION NAME]) is converted into SI.

so now after these three steps we have multple panda data set in which the data set name is the name of the observation, the first row is the vaiable name with si unit and the second two is the least count of it in SI. 

STEP 4 - 
	In this step, the least count is removed from the second row and its coverted into its own coloumn and every observation is saved in parsed_data as parsed_data/[OBSERVATION NAME]_SI.csv for every observation.
basically for [VARIABLE] in observation, a new [VARIABLE]_err is made which basiccally doubles the size of the panda table and then that data is saved as csv in parsed_data.


Just do these 4 steps for now. Ill let you know if there are any issue, make sure to do everythign correct. there shold be no errors . if you dont understand somethign then lmk. don not assume stuff.

===
Okay, everything worked as expected, you did a good job. the only issue is that due to floting point error some values in parsed csv differ form original, like 3.000000000000003 rather than 3.0; tho it works fro me if you can somehow solve this issue than it'd be good
also i forgort to mention the least count to standard deviation erro rshould be done by dividing it by root of 12 not just writing the value, for eg if the least count is 1 then the variable err would be 1/sqrt(12)
tho i think most of the stuff we will do her ewill be seprated, so you dont need to wriet enite thing again jsut let me know what changes I should make/do whatever you prefer. tho always give reasons on why you did what you did do not assume stuff.

Now we will add some plotting functionalities
most of these steps are not related to each other so you can handle them however you like

In this we will mostly do error propogation and stuff. 

Make a image directory. 
now this is where things become unique for each experiment. here we will add extra coloumns to the data set which the analysis requires and also propogate the error


STEP - 1 making of error propogating functions
in this step we make functions which propogate the error.
for exampple for a [VARIABLE], there should be a way to  to all a function and that variable will with its error would get a new coloumn in that data set, like for a variable voltage with error and it is applied to a cube, then add a new row cube_[VARIABLE] and cube_[VAIABLE]_err where the error propogates properly

so i want many function like this, show me some example usage too!. like square., log , exponential etc etc

save as [OBSERVATION]_SI_PARSED.csv in the same directory as before, parsed_data.
STEP - 2 making functions for best fit
this would take input as two coloumn of variable and a function as imput would find the best fit (the best fit should be done by odr so it can account for both x and y errors)
it prints the sigma of fit and other stuff

finally to plot and save the function: it take title as [VARIABLEX] [VARIABLEY] of [OBSERVATION] 
plot x and y , print paras, and close.  

===

For make thse three functions: 1st function will be the data combine second function will work as calculaton of standard error from setup and third functio function will be the latex table writeter
function 1: 
take input as list of dataframe: like: 
['I = 1 Amp, cross hair at chord no leveling', 
'I = 1 Amp, cross hair at diameter no leveling',   
'I = 1 Amp, cross hair at diameter with leveling']:
and a new a new name
checks weather the rows and coloumn set have the same name, combine if it has and saves it. 

function 2: 
this function takes input as just a dataframe as input it makes a new dataframe with new errors, fist calculates sigma  (standard eror of each point which is repeaterd by the ususal formula) and then combines it with the standard error from least count by sqrt(s1^2 s2^2)


lmk if you understand what i meant, also write a python scrypt which uses these function from labtools.py for the 
['I = 1 Amp, cross hair at chord no leveling', 
'I = 1 Amp, cross hair at diameter no leveling',   
'I = 1 Amp, cross hair at diameter with leveling']
first combines them using the new function written and then uses function 2 to create a new dataframe





=== 
